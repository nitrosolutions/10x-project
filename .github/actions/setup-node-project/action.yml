name: Setup Node.js Project
description: Setup Node.js and install dependencies (requires prior checkout)

runs:
  using: composite
  steps:
    - name: Set up Node.js
      uses: actions/setup-node@v6
      with:
        node-version-file: .nvmrc

    - name: Install dependencies
      shell: bash
      run: npm ci
